{
  "name": "{{ plugin_name }}",
  "description": "Epic workflow (brainstorm -> decompose -> plan -> execute -> audit) plus adversarial code and design review, {{ adapter }}-flavored.",
  "version": "{{ marketplace.version }}",
  "author": {
    "name": "{{ marketplace.owner.name }}",
    "email": "{{ marketplace.owner.email }}"
  },
  "homepage": "{{ marketplace.homepage }}",
  "repository": "{{ marketplace.repository }}",
  "license": "{{ marketplace.license }}",
  "keywords": ["workflow", "spec-compliance", "agent-team", "{{ adapter }}"]
}
