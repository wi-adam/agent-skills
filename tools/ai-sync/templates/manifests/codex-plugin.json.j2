{
  "name": "{{ plugin_name }}",
  "version": "{{ marketplace.version }}",
  "description": "Epic workflow plus adversarial review, {{ adapter }}-flavored.",
  "author": {
    "name": "{{ marketplace.owner.name }}",
    "email": "{{ marketplace.owner.email }}"
  },
  "homepage": "{{ marketplace.homepage }}",
  "repository": "{{ marketplace.repository }}",
  "license": "{{ marketplace.license }}",
  "skills": "./skills/",
  "interface": {
    "displayName": "Epic workflow ({{ adapter | title }})",
    "shortDescription": "Spec-compliant epic workflow with cross-model review",
    "category": "Productivity",
    "brandColor": "{{ brand_color }}"
  }
}
